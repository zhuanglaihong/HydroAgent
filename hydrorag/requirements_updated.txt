# HydroRAG系统更新后的依赖

# 向量数据库
chromadb>=0.4.0

# 新的嵌入模型包（推荐）
langchain-huggingface>=0.1.0

# 旧版本嵌入模型（回退用）
langchain-community>=0.0.20

# 直接使用sentence-transformers（最后回退）
sentence-transformers>=2.2.0

# 文档处理
langchain-text-splitters>=0.0.1

# 科学计算
numpy>=1.21.0

# 可选依赖（根据需要安装）

# PDF文档支持
pypdf>=3.0.0

# Word文档支持
python-docx>=0.8.11

# OpenAI嵌入模型支持（可选）
langchain-openai>=0.0.5

# 测试框架
pytest>=7.0.0
pytest-cov>=4.0.0

# 开发工具
black>=22.0.0
flake8>=5.0.0
